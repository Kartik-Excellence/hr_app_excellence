import 'package:flutter/material.dart';

class DialogueBox extends StatelessWidget {
  String message;
  DialogueBox(this.message);
  @override
  Widget build(BuildContext context) {
    return DialogueBox(message);
  }
}
